{
  "name": "claude-session-viewer",
  "version": "1.0.0",
  "description": "A web viewer for Claude Code session history",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jest": "^30.2.0",
    "playwright": "^1.58.2"
  }
}
